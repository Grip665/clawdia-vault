# 2026-08-01 — Jellyfin Stack Full Recovery + systemd Setup

## Context
- Machine rebooted 2026-07-31 23:19 EDT (unclean shutdown) → zurg/rclone died (no systemd), riven-db container exited (255)
- Disk was 92% full → cleaned caches (see below)

## What Was Fixed Tonight

### 1. Disk cleanup (~16GB freed)
- `uv cache clean --force` (14G) — needed --force because Hindsight daemon (uvx hindsight-api, pid 17113) holds the uv cache lock
- `pip cache purge` (3.4G), removed `~/.cache/datalab` (3.3G)
- Disk: 92% → 74% (80G → 64G used)
- Hindsight daemon survived, healthy on port 9077

### 2. Riven DB fix
- Root cause: riven-db (postgres:17-alpine) container exited (255) after reboot, never restarted
- Riven couldn't create database → crash-loop "Failed to create database, exiting"
- Fix: `docker start riven-db` → Postgres recovered cleanly (WAL replay)
- Riven now healthy; Trakt key + Torrentio timeout errors are non-fatal warnings

### 3. Zurg config bug — movies/shows split
- zurg v0.9.3-final (2024 binary) — config used group_order: movies=30, shows=20, other=10
- **zurg semantics: LOWER group_order = checked FIRST** → `other` (regex /.*/) grabbed everything, movies/shows empty
- Fix: other group_order 10 → 40 (catch-all last)
- Result: movies=88, shows=350, other=0, __all__=438
- IMPORTANT: `anime_movies`/`anime_shows` in old mount were stale from old config — removed

### 4. systemd user units (REBOOT-PROOF now!)
- `~/.config/systemd/user/zurg.service` — Type=simple, Restart=on-failure
- `~/.config/systemd/user/rclone-mount.service` — Type=forking, **must include --daemon flag** (without it: start operation timed out), ExecStop=fusermount3 -u
- Both enabled (WantedBy=default.target), linger already enabled for lgf150 → survive reboot
- Docker containers have restart:unless-stopped → auto-start with docker (enabled at boot)

### 5. Jellyfin TV Shows library
- Was configured with NO paths (empty) → 0 series
- Added path: POST /Library/VirtualFolders/Paths {"Name":"TV Shows","Path":"/mount/shows"} (needs MediaPathDto: Name+Path, not bare array)
- **Critical: jellyfin container had to be restarted** after mount was up — bind mount didn't see FUSE mount until restart (`docker restart jellyfin`)
- Scan completed: movies=6,499, series=350, episodes=4,719
- Stream test: HTTP 206 partial content, 2MB in 3.3s ✅

## Connection Details (unchanged)
- Local: http://192.168.50.237:8096, External: http://75.137.103.62:8096
- Login: lgf150 / jellyfin
- API key 3943da53-4181-4f41-ad3f-825634c6708c

## Still Open
- [ ] Riven updater URL → point at Jellyfin correctly (Riven UI localhost:8080)
- [ ] Re-exclude Shakugan no Shana if wanted
- [ ] Test AuthentiCare 3.0 on Orbic RC609L
- [ ] Connect Trakt to Riven (Trakt API key not set)
- [ ] LineageOS flash on E5 Play (weekend)
- [ ] 423 Locked errors in rclone log (Big Bang Theory/One Piece) — RD hoster_unavailable 503s, zurg repairing; may need `clear-torrents` on dead torrents

## Lessons
- zurg group_order: LOWER = first priority. Catch-all dir must have HIGHEST number
- rclone mount systemd: Type=forking REQUIRES --daemon in ExecStart
- Docker bind mounts don't see FUSE mounts mounted after container start → restart container
- riven-db is a Postgres container; check `docker ps -a` for it before blaming Riven
- uv cache clean needs --force when hindsight daemon holds lock (it's fine — tool installs live in ~/.local, not cache)

## 2 PM Auto-Save (cron 660acd84)
- Routine save: graphify update + CAROL_FORM_MAP.txt → Desktop backup
- Git push skipped (Saturday, next run Sunday)
